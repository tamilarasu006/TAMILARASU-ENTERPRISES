const prisma = require('./src/prisma'); const bcrypt = require('bcrypt'); async function run() { try { const hashedPassword = await bcrypt.hash('Password1!', 10); const user = await prisma.user.create({ data: { name: 'Zara', email: 'tamilarasuv006@gmail.com', phone: '+911234567899', password: hashedPassword, emailVerified: false } }); console.log('success', user.email); } catch (e) { console.error('error', e); } } run();
