/**
 * Spring MVC REST controllers exposing the public API.
 *
 * <p>Controllers in this package handle customer-facing endpoints
 * (authentication, product catalog, cart, orders). Admin endpoints
 * are kept in a separate sub-package or prefixed with {@code /admin}.
 */
package com.tamilarasu.ecommerce.controller;
