/**
 * Spring Data JPA repository interfaces for data access.
 *
 * <p>Each repository extends {@code JpaRepository} (or {@code CrudRepository})
 * and provides CRUD operations plus custom query methods for the corresponding
 * entity.
 */
package com.tamilarasu.ecommerce.repository;
